/**
 * Default dimensions for non-responsive chart sizes.
 * @name chart_sizes
 * @memberof config
 * @static
 */
const chart_sizes = {
	card: {
		width: 640,
		height: 390
	},
  auto: {
    width: 640,
    height: 390
  }
};

module.exports = chart_sizes;
