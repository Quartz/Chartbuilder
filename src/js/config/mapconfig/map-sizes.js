/**
 * Default dimensions for non-responsive chart sizes.
 * @name chart_sizes
 * @memberof config
 * @static
 */
const chart_sizes = {
  auto: {
    width: 640,
    height: 390
  }
};

module.exports = chart_sizes;
